---
'@astrojs/react': patch
---

If `include`/`exclude` options are passed to the React integration, use them to filter the components during SSR. This is useful when multiple JSX frameworks are used (e.g. React + Preact).
