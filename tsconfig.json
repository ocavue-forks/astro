{
  "include": [
    // The root `tsconfig.json` includes nothing. All TypeScript files should be included in the `tsconfig.json` in the project references below.
  ],
  "references": [
    { "path": "packages/astro/tsconfig.test.json" },
    { "path": "packages/integrations/react/tsconfig.json" },
    { "path": "packages/integrations/react/tsconfig.test.json" }
  ]
}
